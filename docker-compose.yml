services:
  clipflow:
    build: .
    env_file: .env
    environment:
      - PYTHONPATH=/app
      - PENDING_DIR=/data/pending
    volumes:
      - ${PENDING_DIR}:/data/pending
      - ./secrets:/secrets:ro
